{
  "buildCommand": "npx expo export --platform web && npx workbox-cli generateSW workbox-config.js && node postbuild.js",
  "outputDirectory": "dist",
  "devCommand": "expo",
  "cleanUrls": true,
  "framework": null,
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}